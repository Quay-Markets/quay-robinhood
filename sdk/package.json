{
  "name": "@quay/evm-sdk",
  "version": "0.1.0",
  "description": "Aggregator SDK for the Quay EVM shared-liquidity propAMM: pure quote math (bit-exact vs Solidity), state loading via the on-chain lens, and swap calldata builders. Zero runtime dependencies.",
  "type": "module",
  "license": "BUSL-1.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "test-vectors.json"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "oxlint": "1.73.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
